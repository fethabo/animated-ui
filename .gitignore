node_modules/
dist/

# La raíz se instala con pnpm (ver campo packageManager). El slash inicial
# ancla el patrón a la raíz: docs/ es subproyecto npm y su package-lock.json
# debe seguir trackeado.
/package-lock.json

*.tsbuildinfo
.DS_Store
*.log
coverage/

# Docs: contenido generado en build time (extract-props), nunca se edita a mano
docs/src/generated/
docs/public/sitemap.xml
docs/public/robots.txt
