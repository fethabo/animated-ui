## 1. Actualización de documentación de componentes

- [x] 1.1 Modificar la documentación de `AnimatedBackground` agregando la receta de "CSS Class Mode" y uso de `registerAnimatedBackground()`.
- [x] 1.2 Modificar la documentación de `ShinyText` agregando la receta de "CSS Class Mode" y uso de `registerShinyText()`.
- [x] 1.3 Modificar la documentación de `GlitchText` agregando la receta de "CSS Class Mode" y uso de `registerGlitchText()`.
- [x] 1.4 Modificar la documentación de `BorderBeam` agregando la receta de "CSS Class Mode" y uso de `registerBorderBeam()`.

## 2. Accesibilidad

- [x] 2.1 Asegurarse que la nueva receta de CSS en los 4 componentes contenga la aclaración sobre el atributo `data-aui-motion` para deshabilitar temporalmente el comportamiento `prefers-reduced-motion`.
